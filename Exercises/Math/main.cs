using System;
using static System.Console;
static class main{
	static double x=1.0;
	static int i;
	static string hello = $"Hello, x={x}\n";
	static double times2(double y){
		double x=7;
		{
		double a=9;
		}
		double z=7;
		WriteLine(x);
		return y*2;
	}
	static int Main(){
		Write($"x={x} Sin(x)={System.Math.Sin(x)}\n");
	return 0;
	}
}
